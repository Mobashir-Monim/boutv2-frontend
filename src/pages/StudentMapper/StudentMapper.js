import { pageLayoutStyles } from "../../utils/styles/styles";

const StudentMapper = () => {
    return <div className={`${pageLayoutStyles.scrollable} flex flex-col`}>
        
    </div>
}

export default StudentMapper;